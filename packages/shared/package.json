{
  "name": "shared",
  "packageManager": "yarn@3.6.4",
  "version": "1.0.0",
  "license": "MIT",
  "private": true,
  "scripts": {
    "build": "rimraf dist && tsc",
    "postinstall": "yarn build",
    "watch": "tsc --watch"
  },
  "devDependencies": {
    "rimraf": "^5.0.7"
  }
}
