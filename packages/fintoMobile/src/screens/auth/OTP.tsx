import * as React from 'react';
import {Text, View} from 'react-native';

interface OTPProps {}

export const OTP: React.FC<OTPProps> = ({}) => {
  return (
    <View>
      <Text>OTP</Text>
    </View>
  );
};
