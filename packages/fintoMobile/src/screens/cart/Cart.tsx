import * as React from 'react';
import {Text, View} from 'react-native';

interface CartProps {}

export const Cart: React.FC<CartProps> = ({}) => {
  return (
    <View>
      <Text>Cart</Text>
    </View>
  );
};
