import * as React from 'react';
import {Text, View} from 'react-native';

interface HomeProps {}

export const Home: React.FC<HomeProps> = ({}) => {
  return (
    <View>
      <Text>Home</Text>
    </View>
  );
};
